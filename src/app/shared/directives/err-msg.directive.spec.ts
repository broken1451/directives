import { ErrMsgDirective } from './err-msg.directive';

describe('ErrMsgDirective', () => {
  it('should create an instance', () => {
    const directive = new ErrMsgDirective();
    expect(directive).toBeTruthy();
  });
});
